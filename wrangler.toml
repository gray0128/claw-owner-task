name = "claw-owner-task"
main = "src/worker/index.ts"
compatibility_date = "2024-02-22"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "claw-owner-task-db"
database_id = "your_d1_database_id_here" # Need to replace after `wrangler d1 create`
migrations_dir = "src/worker/db/migrations"

# Environment Variables
[vars]
USER_TIMEZONE = "Asia/Shanghai"
ENABLE_AI = true

[ai]
binding = "AI"

# Scheduled Tasks (Cron Triggers)
# triggers every 1 minute for cloud bark notifications
[triggers]
crons = ["* * * * *"]

[dev]
port = 8787
